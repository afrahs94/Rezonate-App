import 'package:flutter/widgets.dart';

class OBKeys {
  // Home
  static final addHabit = GlobalKey();         // the “+ Add tracker” button
  static final chartSelector = GlobalKey();    // the dropdown icon for chart
  static final journalTab = GlobalKey();       // bottom nav journal
  static final settingsTab = GlobalKey();      // bottom nav settings


  // Community screen
  static final communityComposer = GlobalKey(); // the “start writing” bar / post button

  // Journal screen
  static final addJournal = GlobalKey();        // the FAB “+” (or list add)

  // You can add more as you like.
}
